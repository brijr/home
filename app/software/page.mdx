import bt from "@/lib/brijr";
import { InLink } from "@/components/in-link";
import { Breadcrumb } from "@/components/breadcrumb";

# <Breadcrumb href="/" name="Home" /> Software

## Software I have helped design and develop

<div className="not-prose grid gap-3 mt-12">
  {bt.projects.map((project) => (
    <InLink href={project.href} key={project.label} desc={project.desc}>
      {project.label}
    </InLink>
  ))}
</div>
