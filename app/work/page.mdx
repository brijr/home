import bt from "@/lib/brijr";

# Work

## My work experience, up to now.

<div className="grid gap-2 mt-12">
  {bt.work.map((project) => (
    <div
      className="flex justify-between text-muted-foreground"
      key={project.label}
    >
      <a href={project.href}>{project.label}</a>
    </div>
  ))}
</div>
