import bt from "@/lib/brijr";
import { OutLink } from "@/components/out-link";
import { Breadcrumb } from "@/components/breadcrumb";

# <Breadcrumb href="/" name="Home" /> Work

## My work experience, up to now. View my [resume here](https://resume.bridger.to).

<div className="not-prose grid gap-3 mt-12">
  {bt.work.map((project) => (
    <OutLink href={project.href} key={project.label} desc={project.desc}>
      {project.label}
    </OutLink>
  ))}
</div>
