import bt from "@/lib/brijr";

# Brands

## Brands that I have designed and created.

Coming soon ...

{/* <div className="not-prose grid gap-2 mt-12">
  {bt.os.map((project) => (
    <div className="md:flex justify-between" key={project.label}>
      <a href={project.href}>{project.label}</a>
      <p className="text-muted-foreground">{project.desc}</p>
    </div>
  ))}
</div> */}
