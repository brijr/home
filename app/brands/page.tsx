import bt from "@/lib/brijr";

export default function Page() {
  return (
    <main className="mx-auto max-w-xl ">
      <p>coming soon ...</p>
      {/* <h1>Software</h1>
      {bt.brands.map((project) => (
        <a
          key={project.label}
          href={project.href}
          className="block p-4 my-2 bg-secondary hover:bg-secondary-foreground transition-colors text-foreground rounded-md"
        >
          {project.label}
        </a>
      ))} */}
    </main>
  );
}
